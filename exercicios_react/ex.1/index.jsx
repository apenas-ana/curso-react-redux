import React from 'react'
import ReactDOM from 'react-dom'

ReactDOM.render(
    <h1>Olá React</h1>,
    document.getElementById('app')
)

// Exemplo com jQuery
/*
const list = $('<ul>')
const item = $('<li>').attr(...)
list.append(item1)
*/